@page "/administration/{Operation}"
@page "/admin/{Operation}"

<MudText Typo=@Typo.h3>@Operation</MudText>

@code {

    [Parameter]
    public string Operation { get; set; }

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
    }

}
