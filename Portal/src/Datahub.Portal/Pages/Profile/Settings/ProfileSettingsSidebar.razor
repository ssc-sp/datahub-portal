@using Microsoft.Graph

@inject DatahubPortalConfiguration _datahubPortalConfiguration
@inject NavigationManager _navigationManager

<MudNavMenu Bordered Dense>
    <MudNavLink Icon="@Icons.Material.Filled.Person" Href="@($"/profile/settings/{ProfileSettingsPage.SectionViews.ProfileDisplay}")">
        @Localizer["Profile"]
    </MudNavLink>
    <MudNavLink Icon="@Icons.Material.Filled.Brush" Href="@($"/profile/settings/{ProfileSettingsPage.SectionViews.Appearance}")">
        @Localizer["Appearance"]
    </MudNavLink>
    <MudNavLink Icon="@Icons.Material.Filled.Mail" Href="@($"/profile/settings/{ProfileSettingsPage.SectionViews.Notifications}")">
        @Localizer["Notifications"]
    </MudNavLink>
</MudNavMenu>

@code {
    
    [Parameter]
    public PortalUser ViewedUser { get; set; }
}