@using Datahub.Portal.Pages.Workspace.Reports.Charts
@using Datahub.Shared.Entities
@using System.Text.Json
@using Datahub.Application.Services.Cost
@using Datahub.Core.Model.Projects

@inject IDbContextFactory<DatahubProjectDBContext> _dbContextFactory
@inject DatahubPortalConfiguration _datahubPortalConfiguration
@inject IJSRuntime _jsRuntime


@* <MudDataGrid Items="@_statsRows" ReadOnly="@_isReadOnly" Filterable="true" Striped="true" FilterMode="DataGridFilterMode.ColumnFilterRow">
    <ToolBarContent>
        <MudText Typo="Typo.h2">
            @Localizer["BLAH"]
        </MudText>
        <MudSpacer />
        <DHButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@SidebarIcons.Download"
        OnClick="HandleDownload" Class="mr-4">
            @Localizer["Download to CSV"]
        </DHButton>
    </ToolBarContent>

    <Columns>
        <HierarchyColumn InitiallyExpandedFunc="@(x => false)"/>
        <PropertyColumn Property="x => x.Acronym" Title="Workspace Acroynm"></PropertyColumn>
        <TemplateColumn Title="Workspace Lead">
            <CellTemplate>
                @if(context.Item.Leads.Count > 0)
                {
                    foreach (var user in context.Item.Leads)
                    {
                        <div>@user</div>
                    }
                }

                else
                {
                    <MudText>@Localizer["None"]</MudText>
                }
            </CellTemplate>
        </TemplateColumn>
        <PropertyColumn Property="x => Math.Round(x.BudgetRemaining, 2)" Title="Budget Remaining"></PropertyColumn>
        <PropertyColumn Property="x => x.ProvisionedResources" Title="Provisioned Tools"></PropertyColumn>
    </Columns>

    <ChildRowContent>
        <MudCard>
            <MudCardHeader>
                <CardHeaderContent>
                    <MudText Typo="Typo.h4">@context.Item.Name</MudText>
                </CardHeaderContent>
            </MudCardHeader>
            <MudCardContent>
                <MudStack Row="true">


                </MudStack>


                <MudText Typo="Typo.h6">@Localizer["Budget"]</MudText>

                <MudText Typo="Typo.h6">@Localizer["Users"]</MudText>
                <MudTable Items="@context.Item.AllUsers" Context="AllUsersContext" Elevation="0" Dense>
                    <HeaderContent>
                        <MudTh>
                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                <strong>Name</strong>
                            </MudTableSortLabel>
                        </MudTh>
                        <MudTh>
                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                <strong>Department</strong>
                            </MudTableSortLabel>
                        </MudTh>
                        <MudTh>
                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                <strong>Role</strong>
                            </MudTableSortLabel>
                        </MudTh>
                    </HeaderContent>
                    <RowTemplate>
                        <MudTd DataLabel="AllUsers">
                            @AllUsersContext.User_Name
                        </MudTd>
                        <MudTd DataLabel="Leads">
                            @GetOrg(AllUsersContext.User_Name)
                        </MudTd>
                        <MudTd DataLabel="Leads">
                            @GetRole(AllUsersContext.RoleId)
                        </MudTd>
                    </RowTemplate>
                </MudTable>

                <MudText>

                </MudText>
            </MudCardContent>
        </MudCard>
    </ChildRowContent>

    <PagerContent>
        <MudDataGridPager RowsPerPageString="@Localizer["Rows per page:"]" InfoFormat="@("{first_item}-{last_item} " + Localizer["of"] + " {all_items}")" />
    </PagerContent>

</MudDataGrid> *@


<MudStack>
    <MudText Typo="Typo.h2">DataHub Statistics</MudText>
    <MudText>This page shows statistics about the DataHub.</MudText>
    <MudText Typo="Typo.h2">
        @Localizer["BLAH"]
    </MudText>
    <MudSpacer />
    <DHButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@SidebarIcons.Download"
    OnClick="HandleDownload" Class="mr-4">
        @Localizer["Download to CSV"]
    </DHButton>

    <MudTable Sortable="true" T="DatahubProjectStatsRow" Hover="true" Loading="_isLoading" LoadingProgressColor="Color.Info" ServerData="LoadWorkspaceChecks" @ref="workspaceHealthTable">
        <HeaderContent>
            <MudTh>
                @* ShowMore button column *@
            </MudTh>
            <MudTh>
                <MudTableSortLabel SortBy="@(new Func<DatahubProjectStatsRow, object>(x => x.Name))">Workspace</MudTableSortLabel>
                <MudTextField Value="searchAcronym"
                ValueChanged="SearchAcronymChanged"
                DebounceInterval="300"
                Label=""
                T="string"
                Immediate="true" />
            </MudTh>
            @*             <MudTh>
                <MudTableSortLabel SortBy="@(new Func<DatahubProjectStatsRow, object>(x => x.Name))">Full Name</MudTableSortLabel>
            </MudTh>
            <MudTh>
                <MudTableSortLabel SortBy="@(new Func<DatahubProjectStatsRow, object>(x => x.Leads))">Department(s)</MudTableSortLabel>
            </MudTh> *@
            <MudTh>
                <MudTableSortLabel SortBy="@(new Func<DatahubProjectStatsRow, object>(x => x.Leads))">Department(s)</MudTableSortLabel>
                <MudTextField Value="searchLead"
                ValueChanged="SearchLeadChanged"
                DebounceInterval="300"
                Label=""
                T="string"
                Immediate="true" />
            </MudTh>
            <MudTh>
                <MudTableSortLabel SortBy="@(new Func<DatahubProjectStatsRow, object>(x => x.BudgetRemaining))">Budget Remaining</MudTableSortLabel>
                <MudNumericField Value="percentBudget"
                ValueChanged="PercentBudgetChanged"
                DebounceInterval="300"
                Label=""
                T="int"
                Immediate="true"
                Min="0"/>
            </MudTh>
@*             <MudTh>
                <MudTableSortLabel SortBy="@(new Func<DatahubProjectStatsRow, object>(x => x.MetadataComplete))">Metadata</MudTableSortLabel>
            </MudTh> *@
            <MudTh>
                <MudTableSortLabel SortBy="@(new Func<DatahubProjectStatsRow, object>(x => x.ProvisionedResources))">Provisioned Tools</MudTableSortLabel>
                <MudSelect T="string"
                Value="selectedType"
                ValueChanged=SelectedTypeChanged
                Variant="Variant.Text" Dense="true">
                    <MudSelectItem T="string" Value="null" DataLabel="Lead(s)">@Localizer["All"]</MudSelectItem>
                    @foreach (var status in _resources)
                    {
                        <MudSelectItem T="string" Value="status">@Localizer[status.ToString()]</MudSelectItem>
                    }
                </MudSelect>
            </MudTh>
        </HeaderContent>


        <RowTemplate>
            <MudTd DataLabel="ShowMore">
                <DHButton Size="Size.Small" OnClick="@(() => ShowUsersPressed(context.ProjectId))">
                    @if(context.ShowUserDetails)
                    {
                        <MudIcon Icon="fa fa-chevron-right"></MudIcon>
                    }
                    else
                    {
                        <MudIcon Icon="fa fa-chevron-down"></MudIcon>
                    }

                </DHButton>
            </MudTd>
            <MudTd DataLabel="Workspace">
                <DHButton Href="@($"/{_datahubPortalConfiguration.ProjectUrlSegment}/{context.Acronym}")">
                    <strong> @context.Acronym </strong>
                </DHButton>
            </MudTd>
            @*             <MudTd DataLabel="FullName">
                @context.Name
            </MudTd>
            <MudTd DataLabel="Departments">
                @GetOrg(AllUsersContext.User_Name)
                @foreach (var department in context.Leads)
                {
                    <span>@GetOrg(department.User_Name)</span>
                    <br/>
                    break;
                }
            </MudTd> *@
            <MudTd DataLabel="Leads">
                @foreach (var department in context.Leads)
                {
                    <span>@department</span>
                    <br />
                }
            </MudTd>
            <MudTd DataLabel="Budget">
                <MudTooltip Text="@($"{context.BudgetSpent.ToString("C", CultureInfo.CurrentCulture)} / {context.BudgetLimit.ToString("C", CultureInfo.CurrentCulture)}")">
                    @if(context.BudgetRemaining < 0)
                    {
                        <MudAlert>
                            @context.BudgetRemaining.ToString("C", CultureInfo.CurrentCulture)
                        </MudAlert>
                    }
                    else
                    {
                        @context.BudgetRemaining.ToString("C", CultureInfo.CurrentCulture)
                    }
                </MudTooltip>
            </MudTd>
@*             <MudTd DataLabel="Metadata">
                @if (context.MetadataComplete)
                {
                    <DHIcon Icon="@Icons.Material.Filled.Check" Color="Color.Success" />
                }
                else
                {
                    <DHIcon Icon="@Icons.Material.Filled.WatchLater" Color="Color.Warning" />
                }
            </MudTd> *@
            <MudTd DataLabel="Provisioned Tools">
                @if(context.ProvisionedResources != null)
                {
                    @foreach (var user in context.ProvisionedResources)
                    {
                        <span> @GetLabel(user.ResourceType)</span>
                        <br />
                        Console.WriteLine(user);
                    }
                }

            </MudTd>
        </RowTemplate>

        <ChildRowContent>
            @if (context.ShowUserDetails)
            {
                <MudTr>
                    <td colspan="7">
                        <MudCard Elevation="0">
                            <MudCardHeader>
                                <CardHeaderContent>
                                    <MudText Typo="Typo.body1">User details for <strong>@context.Name</strong></MudText>
                                </CardHeaderContent>
                            </MudCardHeader>
                            <MudCardContent class="pl-6 py-0 pr-0">
                                <MudTable Items="@context.AllUsers" Context="AllUsersContext" Elevation="0" Dense Style="background-color: grey;">
                                    <HeaderContent>
                                        <MudTh>
                                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                                <strong>Name</strong>
                                            </MudTableSortLabel>
                                        </MudTh>
                                        <MudTh>
                                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                                <strong>Email</strong>
                                            </MudTableSortLabel>
                                        </MudTh>
                                        <MudTh>
                                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                                <strong>Role</strong>
                                            </MudTableSortLabel>
                                        </MudTh>
                                    </HeaderContent>
                                    <RowTemplate>
                                        <MudTd DataLabel="AllUsers" Style="background-color: lightgrey;">
                                            @AllUsersContext.User_Name
                                            <br />
                                            Gecelovsky, Siovan
                                        </MudTd>
                                        <MudTd DataLabel="Leads" Style="background-color: lightgrey;">
                                            @AllUsersContext.ProjectUser_ID
                                            <br />
                                            siovan.gecelovsky@ssc-spc.gc.ca
                                        </MudTd>
                                        <MudTd DataLabel="Leads" Style="background-color: lightgrey;">
                                            @AllUsersContext.IsAdmin
                                            <br />
                                            Admin
                                        </MudTd>
                                    </RowTemplate>
                                </MudTable>
                            </MudCardContent>
                        </MudCard>
                    </td>
                </MudTr>
            }

            @if (context.ShowBudgetDetails)
            {
                <MudTr>
                    <td colspan="7">
                        <MudCard Elevation="0">
                            <MudCardHeader>
                                <CardHeaderContent>
                                    <MudText Typo="Typo.body1">Budget details for <strong>@context.Name</strong></MudText>
                                </CardHeaderContent>
                            </MudCardHeader>
                            <MudCardContent class="pl-6 py-0 pr-0">
                                <MudTable Items="@context.Leads" Context="LeadContext" Elevation="0" Dense>
                                    <HeaderContent>
                                        <MudTh>
                                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                                <strong>Cost of last 7 days</strong>
                                            </MudTableSortLabel>
                                        </MudTh>
                                    </HeaderContent>
                                    <RowTemplate>
                                        <MudTd DataLabel="Leads">
                                            @LeadContext
                                        </MudTd>
                                    </RowTemplate>
                                </MudTable>
                                <MudTable Items="@context.Admins" Context="AdminContext" Elevation="0" Dense>
                                    <HeaderContent>
                                        <MudTh>
                                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                                <strong>Budget</strong>
                                            </MudTableSortLabel>
                                        </MudTh>
                                    </HeaderContent>
                                    <RowTemplate>
                                        <MudTd DataLabel="Admins">
                                            @AdminContext
                                        </MudTd>
                                    </RowTemplate>
                                </MudTable>
                                <MudTable Items="@context.Collaborators" Context="CollaboratorContext" Elevation="0" Dense>
                                    <HeaderContent>
                                        <MudTh>
                                            <MudTableSortLabel SortBy="@(new Func<string, object>(x => x))">
                                                <strong>Provisioned Resources</strong>
                                            </MudTableSortLabel>
                                        </MudTh>
                                    </HeaderContent>
                                    <RowTemplate>
                                        <MudTd DataLabel="Collaborators">
                                            @CollaboratorContext
                                        </MudTd>
                                    </RowTemplate>
                                </MudTable>
                            </MudCardContent>
                        </MudCard>
                    </td>
                </MudTr>
            }
        </ChildRowContent>


        <PagerContent>
            <MudTablePager RowsPerPageString="@Localizer["Rows per page:"]" InfoFormat="@("{first_item}-{last_item} " + Localizer["of"] + " {all_items}")" />
        </PagerContent>
    </MudTable>
</MudStack>

@code {

    [Parameter]
    public Datahub_Project Project { get; set; }

    private bool _isLoading = true;
    private bool _isReadOnly = true;
    private IJSObjectReference _module;

    private List<DatahubProjectStatsRow> _statsRows = new();
    private List<DatahubProjectStatsRow> _filteredItems = new();
    private List<DatahubProjectStatsRow> _workspaceInfrastructureHealthData = new();

    private string searchAcronym = "", searchLead = "";
    private int percentBudget = -1;
    private string? selectedType;

    private MudTable<DatahubProjectStatsRow> workspaceHealthTable;


    private List<string> _resources =
    [
        TerraformTemplate.AzureDatabricks,
    TerraformTemplate.AzureStorageBlob,
    TerraformTemplate.AzureAppService,
    TerraformTemplate.AzurePostgres,
    TerraformTemplate.AzureArcGis,
    TerraformTemplate.AzureAPI
    ];


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await base.OnAfterRenderAsync(firstRender);

        if (firstRender)
        {
            _module = await _jsRuntime.InvokeAsync<IJSObjectReference>("import", "./Pages/Tools/Statistics/StatisticsPage.razor.js");


            await using var context = await _dbContextFactory.CreateDbContextAsync();
            const int numberOfDays = 7;


            _statsRows = context.Projects
                .Include(p => p.Credits)
                .Include(p => p.Users)
                //.Include(p => p.Resources)
                // .Include(p => p.ProjectRequestAudits)
                .AsEnumerable()
                .Select(p => new DatahubProjectStatsRow()
                {
                    ProjectId = p.Project_ID,
                    Name = p.Project_Name,
                    Acronym = p.Project_Acronym_CD,
                    MetadataComplete = p.MetadataAdded ?? false,
                        BudgetLimit = p.Project_Budget ?? 0,
                    BudgetSpent = p.Credits?.Current ?? 0,
                    CostOfLastXDays = GetLastXDaysOfCosts(p.Credits, numberOfDays),

                        AllUsers = p.Users?.ToList(),
                        ProvisionedResources = p.Resources?.ToList(),

                    Leads = p.Users?.Where(u => u.RoleId == (int) Project_Role.RoleNames.WorkspaceLead).Select(u => u.User_Name).ToList(),
                    Admins = p.Users?.Where(u => u.RoleId == (int) Project_Role.RoleNames.Admin).Select(u => u.User_Name).ToList(),
                    Collaborators = p.Users?.Where(u => u.RoleId == (int) Project_Role.RoleNames.Collaborator).Select(u => u.User_Name).ToList(),
                    StorageStatus = p.Resources?
                        .Where(sr => sr.ResourceType.Contains(TerraformTemplate.AzureStorageBlob))
                        .Select(sr => sr.CreatedAt.HasValue ? DatahubProjectStatsRow.ResourceStatus.Complete : DatahubProjectStatsRow.ResourceStatus.Requested)
                        .FirstOrDefault() ?? DatahubProjectStatsRow.ResourceStatus.None,
                    DatabricksStatus = p.Resources?
                        .Where(sr => sr.ResourceType.Contains(TerraformTemplate.AzureDatabricks))
                        .Select(sr => sr.CreatedAt.HasValue ? DatahubProjectStatsRow.ResourceStatus.Complete : DatahubProjectStatsRow.ResourceStatus.Requested)
                        .FirstOrDefault() ?? DatahubProjectStatsRow.ResourceStatus.None
                })
                .ToList();

            _workspaceInfrastructureHealthData = _statsRows;

            _isLoading = false;
            StateHasChanged();
        }
    }

    private async Task SelectedTypeChanged(string resource)
    {
        selectedType = resource;
        await ReloadWorkspaceHealthCheckTableAsync();
    }



    private async Task SearchAcronymChanged(string search)
    {
        searchAcronym = search;
        await ReloadWorkspaceHealthCheckTableAsync();
    }

    private async Task SearchLeadChanged(string search)
    {
        searchLead = search;
        await ReloadWorkspaceHealthCheckTableAsync();
    }

    private async Task PercentBudgetChanged(int percent)
    {
        percentBudget = percent;
        await ReloadWorkspaceHealthCheckTableAsync();
    }

    private async Task ReloadWorkspaceHealthCheckTableAsync()
    {
        if (workspaceHealthTable is not null)
        {
            await workspaceHealthTable.ReloadServerData();
        }
    }

    private async Task<TableData<DatahubProjectStatsRow>> LoadWorkspaceChecks(TableState state, CancellationToken cancellationToken)
    {
        var filteredData = _workspaceInfrastructureHealthData.AsEnumerable();

        // Apply type filter
        // if (!string.IsNullOrEmpty(selectedType))
        // {
        //     filteredData = filteredData.Where(check => check.ProvisionedResources.Contains(selectedType));
        // }


        // Apply search filter
        if (!string.IsNullOrEmpty(searchAcronym))
        {
            filteredData = filteredData.Where(check => check.Acronym.Contains(searchAcronym, StringComparison.OrdinalIgnoreCase));
        }

        //Apply budget filter
        if (percentBudget > -1)
        {
            filteredData = filteredData.Where(check => (check.BudgetRemaining) <= percentBudget);
        }
        
        // Apply sorting
        if (state.SortDirection != SortDirection.None)
        {
            Func<DatahubProjectStatsRow, object> sortBy = state.SortLabel switch
            {
                nameof(DatahubProjectStatsRow.Acronym) => x => x.Acronym,
                nameof(DatahubProjectStatsRow.Name) => x => x.Name,
                nameof(DatahubProjectStatsRow.BudgetRemaining) => x => x.BudgetRemaining,
                _ => x => x.Name
            };

            filteredData = filteredData.OrderByDirection(state.SortDirection, sortBy);
        }

        // Apply pagination
        var totalItems = filteredData.Count();
        var pagedData = filteredData
            .Skip(state.Page * state.PageSize)
            .Take(state.PageSize)
            .ToList();

        return await Task.FromResult(new TableData<DatahubProjectStatsRow> { TotalItems = totalItems, Items = pagedData });
    }

    private void ShowUsersPressed(int projectId)
    {
        var row = _statsRows.First(r => r.ProjectId == projectId);
        row.ShowUserDetails = !row.ShowUserDetails;
    }

    private static decimal GetLastXDaysOfCosts(Project_Credits projectCredits, int days)
    {
        if (projectCredits == null || projectCredits.CurrentPerDay == null)
        {
            return 0;
        }

        var currentPerDay = JsonSerializer.Deserialize(projectCredits.CurrentPerDay, typeof(List<DailyServiceCost>)) as List<DailyServiceCost>;
        var cost = currentPerDay?.Where(c => c.Date > DateTime.Now.AddDays(-days)).Sum(c => c.Amount) ?? 0;

        return cost;
    }


    private string GetOrg(string email)
    {
        if(email==null)
        {
            return "unknown";
        }
        var org = "";
        try
        {
            org = email.Split("@")[1].Split('.')[0];
        }
        catch
        {
            org = "unknown";
        }

        return org;
    }

    private string GetRole(int? num)
    {
        if(num == null)
        {
            return "None";
        }
        switch(num)
        {
            case (int)Project_Role.RoleNames.Admin:
                return "Admin";
            case (int)Project_Role.RoleNames.WorkspaceLead:
                return "Lead";
            case (int)Project_Role.RoleNames.Collaborator:
                return "Collaborator";
            default:
                return "Guest";
        }
    }

    private string GetLabel(string tool)
    {
        return tool switch
        {
            TerraformTemplate.AzureDatabricks => Localizer["Azure Databricks"],
            TerraformTemplate.AzureStorageBlob => Localizer["Azure Storage Blob"],
            TerraformTemplate.AzureAppService => Localizer["Web Application"],
            TerraformTemplate.AzurePostgres => Localizer["Azure Postgres"],
            TerraformTemplate.AzureArcGis => Localizer["Azure ArcGIS"],
            TerraformTemplate.AzureAPI => Localizer["Azure API Management"],
            _ => tool
        };
    }

    // CSV download
    private async Task HandleDownload()
    {
        var fileStream = GenerateCsvStream();
        var fileName = $"WorkspaceStatistics_{DateTime.Now.ToString("yyyy-MM-dd")}.csv";
        using var streamReference = new DotNetStreamReference(stream: fileStream);
        await _module.InvokeVoidAsync("downloadFileFromStream", fileName, streamReference);
    }

    private Stream GenerateCsvStream()
    {
        var stream = new MemoryStream();
        var writer = new StreamWriter(stream);
        writer.WriteLine("Acroynm,Name,Lead,Budget Remaining");
        foreach (var cost in _statsRows)
        {
            writer.WriteLine($"{cost.Acronym},{cost.Name},{cost.Leads},{cost.BudgetRemaining}");
        }

        writer.Flush();
        stream.Position = 0;
        return stream;
    }




}