@attribute [Route(PageRoutes.ExternalInvitationExpired)]
@attribute [Route(PageRoutes.ExternalInvitationExpired_FR)]
@layout HeaderPublicLayout

<MudText Typo="Typo.h5">
    @Localizer["The invitation code has expired and the onboarding process cannot be completed. Please contact the workspace lead to send a new invitation."]
</MudText>

@code {

}
