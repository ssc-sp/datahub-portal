@namespace NRCan.Datahub.Shared.Templates

<h3>@Service Service Request Approved</h3>

<p>Your request for the creation of service <b>@Service</b> in data project <b>@DataProject.ProjectNameEn</b> has been approved. 
    The service links in the <a href=@ProjectPageUrl >data project</a> page will now be active.</p>

<hr />

<h3>Demande de Service Approuvée</h3>

<p>Votre demande pour la création du service <b>@Service</b> dans le projet de données <b>@DataProject.ProjectNameFr</b> a été approuvée. 
    Les liens menant au service dans la page du <a href=@ProjectPageUrl >projet de données</a> seront maintenant actifs.</p>

@code {
    public static readonly string PROJECT_PAGE_PREFIX = "/project";

    [Parameter]
    public string Service { get; set; }

    [Parameter]
    public DatahubProjectInfo DataProject { get; set; }

    [Parameter]
    public string ProjectPageUrl { get; set; }

}
